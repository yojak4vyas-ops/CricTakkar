{
  "name": "crictakkar-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Scheduled jobs for CricTakkar (push notifications, host-less tournaments), run by GitHub Actions. Not part of the deployed website.",
  "main": "send-notifications.js",
  "scripts": {
    "send": "node send-notifications.js",
    "run-tournaments": "node run-scheduled-tournaments.js"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0"
  }
}
